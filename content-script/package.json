{
  "name": "content-script",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "clean": "del -f coverage ../extension/content-script",
    "format": "prettier --write src",
    "lint": "eslint --ext=.ts,.tsx src",
    "test": "jest",
    "coverage": "jest --coverage",
    "build": "node esbuild.js",
    "build:watch": "ESBUILD_WATCH=true yarn build"
  },
  "dependencies": {
    "tslib": "^2.4.0",
    "@extension/utils": "1.0.0"
  }
}
